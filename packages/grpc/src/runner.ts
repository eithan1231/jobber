// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v3.21.12
// source: runner.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "Runner";

export interface HttpHeaders {
  name: string;
  value: string;
}

export interface HttpBody {
  id: string;
  seq: number;
  data: Uint8Array;
  end: boolean;
}

export interface HttpRequestHead {
  id: string;
  method: string;
  scheme: string;
  hostname: string;
  path: string;
  headers: HttpHeaders[];
}

export interface HttpResponseHead {
  id: string;
  status: number;
  headers: HttpHeaders[];
}

export interface HttpEventRequest {
  reqHead?: HttpRequestHead | undefined;
  reqBody?: HttpBody | undefined;
}

export interface HttpEventResponse {
  resHead?: HttpResponseHead | undefined;
  resBody?: HttpBody | undefined;
}

export interface MqttEventRequest {
  topic: string;
  payload: Uint8Array;
}

export interface MqttEventResponse {
  status: MqttEventResponse_Status;
}

export const MqttEventResponse_Status = {
  ACCEPTED: "ACCEPTED",
  REJECTED: "REJECTED",
  UNRECOGNIZED: "UNRECOGNIZED",
} as const;

export type MqttEventResponse_Status = typeof MqttEventResponse_Status[keyof typeof MqttEventResponse_Status];

export namespace MqttEventResponse_Status {
  export type ACCEPTED = typeof MqttEventResponse_Status.ACCEPTED;
  export type REJECTED = typeof MqttEventResponse_Status.REJECTED;
  export type UNRECOGNIZED = typeof MqttEventResponse_Status.UNRECOGNIZED;
}

export function mqttEventResponse_StatusFromJSON(object: any): MqttEventResponse_Status {
  switch (object) {
    case 0:
    case "ACCEPTED":
      return MqttEventResponse_Status.ACCEPTED;
    case 1:
    case "REJECTED":
      return MqttEventResponse_Status.REJECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MqttEventResponse_Status.UNRECOGNIZED;
  }
}

export function mqttEventResponse_StatusToJSON(object: MqttEventResponse_Status): string {
  switch (object) {
    case MqttEventResponse_Status.ACCEPTED:
      return "ACCEPTED";
    case MqttEventResponse_Status.REJECTED:
      return "REJECTED";
    case MqttEventResponse_Status.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function mqttEventResponse_StatusToNumber(object: MqttEventResponse_Status): number {
  switch (object) {
    case MqttEventResponse_Status.ACCEPTED:
      return 0;
    case MqttEventResponse_Status.REJECTED:
      return 1;
    case MqttEventResponse_Status.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface ScheduleEventRequest {
  id: string;
  name: string;
  scheduledTime: number;
}

export interface ScheduleEventResponse {
  status: ScheduleEventResponse_Status;
}

export const ScheduleEventResponse_Status = {
  ACCEPTED: "ACCEPTED",
  REJECTED: "REJECTED",
  UNRECOGNIZED: "UNRECOGNIZED",
} as const;

export type ScheduleEventResponse_Status =
  typeof ScheduleEventResponse_Status[keyof typeof ScheduleEventResponse_Status];

export namespace ScheduleEventResponse_Status {
  export type ACCEPTED = typeof ScheduleEventResponse_Status.ACCEPTED;
  export type REJECTED = typeof ScheduleEventResponse_Status.REJECTED;
  export type UNRECOGNIZED = typeof ScheduleEventResponse_Status.UNRECOGNIZED;
}

export function scheduleEventResponse_StatusFromJSON(object: any): ScheduleEventResponse_Status {
  switch (object) {
    case 0:
    case "ACCEPTED":
      return ScheduleEventResponse_Status.ACCEPTED;
    case 1:
    case "REJECTED":
      return ScheduleEventResponse_Status.REJECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ScheduleEventResponse_Status.UNRECOGNIZED;
  }
}

export function scheduleEventResponse_StatusToJSON(object: ScheduleEventResponse_Status): string {
  switch (object) {
    case ScheduleEventResponse_Status.ACCEPTED:
      return "ACCEPTED";
    case ScheduleEventResponse_Status.REJECTED:
      return "REJECTED";
    case ScheduleEventResponse_Status.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function scheduleEventResponse_StatusToNumber(object: ScheduleEventResponse_Status): number {
  switch (object) {
    case ScheduleEventResponse_Status.ACCEPTED:
      return 0;
    case ScheduleEventResponse_Status.REJECTED:
      return 1;
    case ScheduleEventResponse_Status.UNRECOGNIZED:
    default:
      return -1;
  }
}

function createBaseHttpHeaders(): HttpHeaders {
  return { name: "", value: "" };
}

export const HttpHeaders: MessageFns<HttpHeaders> = {
  encode(message: HttpHeaders, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpHeaders {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpHeaders();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpHeaders {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: HttpHeaders): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<HttpHeaders>): HttpHeaders {
    return HttpHeaders.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HttpHeaders>): HttpHeaders {
    const message = createBaseHttpHeaders();
    message.name = object.name ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseHttpBody(): HttpBody {
  return { id: "", seq: 0, data: new Uint8Array(0), end: false };
}

export const HttpBody: MessageFns<HttpBody> = {
  encode(message: HttpBody, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.seq !== 0) {
      writer.uint32(16).uint64(message.seq);
    }
    if (message.data.length !== 0) {
      writer.uint32(26).bytes(message.data);
    }
    if (message.end !== false) {
      writer.uint32(32).bool(message.end);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpBody {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpBody();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.seq = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.end = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpBody {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      seq: isSet(object.seq) ? globalThis.Number(object.seq) : 0,
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      end: isSet(object.end) ? globalThis.Boolean(object.end) : false,
    };
  },

  toJSON(message: HttpBody): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.seq !== 0) {
      obj.seq = Math.round(message.seq);
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.end !== false) {
      obj.end = message.end;
    }
    return obj;
  },

  create(base?: DeepPartial<HttpBody>): HttpBody {
    return HttpBody.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HttpBody>): HttpBody {
    const message = createBaseHttpBody();
    message.id = object.id ?? "";
    message.seq = object.seq ?? 0;
    message.data = object.data ?? new Uint8Array(0);
    message.end = object.end ?? false;
    return message;
  },
};

function createBaseHttpRequestHead(): HttpRequestHead {
  return { id: "", method: "", scheme: "", hostname: "", path: "", headers: [] };
}

export const HttpRequestHead: MessageFns<HttpRequestHead> = {
  encode(message: HttpRequestHead, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.method !== "") {
      writer.uint32(18).string(message.method);
    }
    if (message.scheme !== "") {
      writer.uint32(26).string(message.scheme);
    }
    if (message.hostname !== "") {
      writer.uint32(34).string(message.hostname);
    }
    if (message.path !== "") {
      writer.uint32(42).string(message.path);
    }
    for (const v of message.headers) {
      HttpHeaders.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpRequestHead {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpRequestHead();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.hostname = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.headers.push(HttpHeaders.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpRequestHead {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
      hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      headers: globalThis.Array.isArray(object?.headers) ? object.headers.map((e: any) => HttpHeaders.fromJSON(e)) : [],
    };
  },

  toJSON(message: HttpRequestHead): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    if (message.hostname !== "") {
      obj.hostname = message.hostname;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.headers?.length) {
      obj.headers = message.headers.map((e) => HttpHeaders.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<HttpRequestHead>): HttpRequestHead {
    return HttpRequestHead.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HttpRequestHead>): HttpRequestHead {
    const message = createBaseHttpRequestHead();
    message.id = object.id ?? "";
    message.method = object.method ?? "";
    message.scheme = object.scheme ?? "";
    message.hostname = object.hostname ?? "";
    message.path = object.path ?? "";
    message.headers = object.headers?.map((e) => HttpHeaders.fromPartial(e)) || [];
    return message;
  },
};

function createBaseHttpResponseHead(): HttpResponseHead {
  return { id: "", status: 0, headers: [] };
}

export const HttpResponseHead: MessageFns<HttpResponseHead> = {
  encode(message: HttpResponseHead, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    for (const v of message.headers) {
      HttpHeaders.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpResponseHead {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpResponseHead();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.headers.push(HttpHeaders.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpResponseHead {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      headers: globalThis.Array.isArray(object?.headers) ? object.headers.map((e: any) => HttpHeaders.fromJSON(e)) : [],
    };
  },

  toJSON(message: HttpResponseHead): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.headers?.length) {
      obj.headers = message.headers.map((e) => HttpHeaders.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<HttpResponseHead>): HttpResponseHead {
    return HttpResponseHead.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HttpResponseHead>): HttpResponseHead {
    const message = createBaseHttpResponseHead();
    message.id = object.id ?? "";
    message.status = object.status ?? 0;
    message.headers = object.headers?.map((e) => HttpHeaders.fromPartial(e)) || [];
    return message;
  },
};

function createBaseHttpEventRequest(): HttpEventRequest {
  return { reqHead: undefined, reqBody: undefined };
}

export const HttpEventRequest: MessageFns<HttpEventRequest> = {
  encode(message: HttpEventRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reqHead !== undefined) {
      HttpRequestHead.encode(message.reqHead, writer.uint32(10).fork()).join();
    }
    if (message.reqBody !== undefined) {
      HttpBody.encode(message.reqBody, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpEventRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpEventRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reqHead = HttpRequestHead.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reqBody = HttpBody.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpEventRequest {
    return {
      reqHead: isSet(object.reqHead) ? HttpRequestHead.fromJSON(object.reqHead) : undefined,
      reqBody: isSet(object.reqBody) ? HttpBody.fromJSON(object.reqBody) : undefined,
    };
  },

  toJSON(message: HttpEventRequest): unknown {
    const obj: any = {};
    if (message.reqHead !== undefined) {
      obj.reqHead = HttpRequestHead.toJSON(message.reqHead);
    }
    if (message.reqBody !== undefined) {
      obj.reqBody = HttpBody.toJSON(message.reqBody);
    }
    return obj;
  },

  create(base?: DeepPartial<HttpEventRequest>): HttpEventRequest {
    return HttpEventRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HttpEventRequest>): HttpEventRequest {
    const message = createBaseHttpEventRequest();
    message.reqHead = (object.reqHead !== undefined && object.reqHead !== null)
      ? HttpRequestHead.fromPartial(object.reqHead)
      : undefined;
    message.reqBody = (object.reqBody !== undefined && object.reqBody !== null)
      ? HttpBody.fromPartial(object.reqBody)
      : undefined;
    return message;
  },
};

function createBaseHttpEventResponse(): HttpEventResponse {
  return { resHead: undefined, resBody: undefined };
}

export const HttpEventResponse: MessageFns<HttpEventResponse> = {
  encode(message: HttpEventResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resHead !== undefined) {
      HttpResponseHead.encode(message.resHead, writer.uint32(10).fork()).join();
    }
    if (message.resBody !== undefined) {
      HttpBody.encode(message.resBody, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpEventResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpEventResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resHead = HttpResponseHead.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resBody = HttpBody.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpEventResponse {
    return {
      resHead: isSet(object.resHead) ? HttpResponseHead.fromJSON(object.resHead) : undefined,
      resBody: isSet(object.resBody) ? HttpBody.fromJSON(object.resBody) : undefined,
    };
  },

  toJSON(message: HttpEventResponse): unknown {
    const obj: any = {};
    if (message.resHead !== undefined) {
      obj.resHead = HttpResponseHead.toJSON(message.resHead);
    }
    if (message.resBody !== undefined) {
      obj.resBody = HttpBody.toJSON(message.resBody);
    }
    return obj;
  },

  create(base?: DeepPartial<HttpEventResponse>): HttpEventResponse {
    return HttpEventResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HttpEventResponse>): HttpEventResponse {
    const message = createBaseHttpEventResponse();
    message.resHead = (object.resHead !== undefined && object.resHead !== null)
      ? HttpResponseHead.fromPartial(object.resHead)
      : undefined;
    message.resBody = (object.resBody !== undefined && object.resBody !== null)
      ? HttpBody.fromPartial(object.resBody)
      : undefined;
    return message;
  },
};

function createBaseMqttEventRequest(): MqttEventRequest {
  return { topic: "", payload: new Uint8Array(0) };
}

export const MqttEventRequest: MessageFns<MqttEventRequest> = {
  encode(message: MqttEventRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.topic !== "") {
      writer.uint32(10).string(message.topic);
    }
    if (message.payload.length !== 0) {
      writer.uint32(18).bytes(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MqttEventRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMqttEventRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.topic = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MqttEventRequest {
    return {
      topic: isSet(object.topic) ? globalThis.String(object.topic) : "",
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
    };
  },

  toJSON(message: MqttEventRequest): unknown {
    const obj: any = {};
    if (message.topic !== "") {
      obj.topic = message.topic;
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create(base?: DeepPartial<MqttEventRequest>): MqttEventRequest {
    return MqttEventRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MqttEventRequest>): MqttEventRequest {
    const message = createBaseMqttEventRequest();
    message.topic = object.topic ?? "";
    message.payload = object.payload ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMqttEventResponse(): MqttEventResponse {
  return { status: MqttEventResponse_Status.ACCEPTED };
}

export const MqttEventResponse: MessageFns<MqttEventResponse> = {
  encode(message: MqttEventResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== MqttEventResponse_Status.ACCEPTED) {
      writer.uint32(8).int32(mqttEventResponse_StatusToNumber(message.status));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MqttEventResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMqttEventResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = mqttEventResponse_StatusFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MqttEventResponse {
    return {
      status: isSet(object.status)
        ? mqttEventResponse_StatusFromJSON(object.status)
        : MqttEventResponse_Status.ACCEPTED,
    };
  },

  toJSON(message: MqttEventResponse): unknown {
    const obj: any = {};
    if (message.status !== MqttEventResponse_Status.ACCEPTED) {
      obj.status = mqttEventResponse_StatusToJSON(message.status);
    }
    return obj;
  },

  create(base?: DeepPartial<MqttEventResponse>): MqttEventResponse {
    return MqttEventResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MqttEventResponse>): MqttEventResponse {
    const message = createBaseMqttEventResponse();
    message.status = object.status ?? MqttEventResponse_Status.ACCEPTED;
    return message;
  },
};

function createBaseScheduleEventRequest(): ScheduleEventRequest {
  return { id: "", name: "", scheduledTime: 0 };
}

export const ScheduleEventRequest: MessageFns<ScheduleEventRequest> = {
  encode(message: ScheduleEventRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.scheduledTime !== 0) {
      writer.uint32(24).uint64(message.scheduledTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduleEventRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduleEventRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.scheduledTime = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduleEventRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      scheduledTime: isSet(object.scheduledTime) ? globalThis.Number(object.scheduledTime) : 0,
    };
  },

  toJSON(message: ScheduleEventRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.scheduledTime !== 0) {
      obj.scheduledTime = Math.round(message.scheduledTime);
    }
    return obj;
  },

  create(base?: DeepPartial<ScheduleEventRequest>): ScheduleEventRequest {
    return ScheduleEventRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScheduleEventRequest>): ScheduleEventRequest {
    const message = createBaseScheduleEventRequest();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.scheduledTime = object.scheduledTime ?? 0;
    return message;
  },
};

function createBaseScheduleEventResponse(): ScheduleEventResponse {
  return { status: ScheduleEventResponse_Status.ACCEPTED };
}

export const ScheduleEventResponse: MessageFns<ScheduleEventResponse> = {
  encode(message: ScheduleEventResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== ScheduleEventResponse_Status.ACCEPTED) {
      writer.uint32(8).int32(scheduleEventResponse_StatusToNumber(message.status));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduleEventResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduleEventResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = scheduleEventResponse_StatusFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduleEventResponse {
    return {
      status: isSet(object.status)
        ? scheduleEventResponse_StatusFromJSON(object.status)
        : ScheduleEventResponse_Status.ACCEPTED,
    };
  },

  toJSON(message: ScheduleEventResponse): unknown {
    const obj: any = {};
    if (message.status !== ScheduleEventResponse_Status.ACCEPTED) {
      obj.status = scheduleEventResponse_StatusToJSON(message.status);
    }
    return obj;
  },

  create(base?: DeepPartial<ScheduleEventResponse>): ScheduleEventResponse {
    return ScheduleEventResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScheduleEventResponse>): ScheduleEventResponse {
    const message = createBaseScheduleEventResponse();
    message.status = object.status ?? ScheduleEventResponse_Status.ACCEPTED;
    return message;
  },
};

export type RunnerDefinition = typeof RunnerDefinition;
export const RunnerDefinition = {
  name: "Runner",
  fullName: "Runner.Runner",
  methods: {
    eventHttp: {
      name: "eventHttp",
      requestType: HttpEventRequest,
      requestStream: true,
      responseType: HttpEventResponse,
      responseStream: true,
      options: {},
    },
    eventMqtt: {
      name: "eventMqtt",
      requestType: MqttEventRequest,
      requestStream: false,
      responseType: MqttEventResponse,
      responseStream: false,
      options: {},
    },
    eventSchedule: {
      name: "eventSchedule",
      requestType: ScheduleEventRequest,
      requestStream: false,
      responseType: ScheduleEventResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface RunnerServiceImplementation<CallContextExt = {}> {
  eventHttp(
    request: AsyncIterable<HttpEventRequest>,
    context: CallContext & CallContextExt,
  ): ServerStreamingMethodResult<DeepPartial<HttpEventResponse>>;
  eventMqtt(request: MqttEventRequest, context: CallContext & CallContextExt): Promise<DeepPartial<MqttEventResponse>>;
  eventSchedule(
    request: ScheduleEventRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ScheduleEventResponse>>;
}

export interface RunnerClient<CallOptionsExt = {}> {
  eventHttp(
    request: AsyncIterable<DeepPartial<HttpEventRequest>>,
    options?: CallOptions & CallOptionsExt,
  ): AsyncIterable<HttpEventResponse>;
  eventMqtt(request: DeepPartial<MqttEventRequest>, options?: CallOptions & CallOptionsExt): Promise<MqttEventResponse>;
  eventSchedule(
    request: DeepPartial<ScheduleEventRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ScheduleEventResponse>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export type ServerStreamingMethodResult<Response> = { [Symbol.asyncIterator](): AsyncIterator<Response, void> };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
