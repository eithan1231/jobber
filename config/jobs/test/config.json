{
  "name": "test",
  "description": "I am a test",

  "version": "1",

  "execution": {
    "conditions": [
      {
        "type": "schedule",
        "cron": "0,30 * * * * *"
      }
    ],
    "action": {
      "type": "script",
      "keepAlive": false,
      "script": "export const handler = (payload) => 'just testing here bruv'"
    }
  }
}
